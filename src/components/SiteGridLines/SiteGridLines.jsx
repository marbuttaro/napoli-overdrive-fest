// Griglia quadrata a 4 colonne con una croce a ogni intersezione: interamente in CSS
// (vedi .site-grid-lines in styles/global.css), qui solo il contenitore che la aggancia.
const SiteGridLines = () => <div className="site-grid-lines" aria-hidden="true" />;

export default SiteGridLines;
